<h1>Welcome to the Burger Joint</h1>

<h2>Check out all the burgers on our menus, even suggest some of your own.</h2>

<ul>
    {{#each burger}}
        {{#unless devoured}}
            {{> burgers/burger-block devoured=true}}
        {{/unless}}
    {{/each}}
</ul>

<h2>Sorry, these burgers have been eaten already!</h2>

<ul>
    {{#each burgers}}
        {{#if devoured}}
            {{> burgers/burger-block devoured=false}}
        {{/if}}
    {{/each}}
</ul>

<h2>Suggest a Burger for our Menu!</h2>

<form class="create-form">
    <div class="form-group">
        <label for="bn">Burger Name:</label>
        <input type="text" id="bn" name="name">
    </div>
    <div class="form-group">
        <label for="dev">Devoured?</label>
        <input type="radio" name="devoured" value="1" checked> Devoured!<br>
        <input type="radio" name="devoured" value="0">Not Yet Eaten
    </div>

    <button type="submit">Add Burger</button>
</form>